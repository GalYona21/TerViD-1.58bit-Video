# Core — pinned for PyTorch 2.4 + CUDA 12.1 compatibility
# Install torch/torchvision first:
#   pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu121
# Then:
#   pip install -r requirements.txt

accelerate>=0.33.0,<1.0
diffusers>=0.30.0,<0.32.0
transformers>=4.44.0,<5.0
safetensors>=0.4.0

# Training
deepspeed>=0.12.0
wandb>=0.16.0

# Data
video2dataset>=1.1.0
webdataset>=0.2.0
decord>=0.6.0

# Evaluation
clip-benchmark>=1.4.0

# Utils
pyyaml>=6.0
tqdm>=4.65.0
einops>=0.7.0
